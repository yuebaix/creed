server:
  port: 10203
spring:
  profiles:
    active: dev
  application:
    name: sc-consul-service
  cloud:
    consul:
      scheme: http
      host: localhost
      port: 8500
      discovery:
        prefer-ip-address: true
        instance-id: ${spring.application.name}:${spring.cloud.client.ip-address}:${server.port}
        instance-group: dev
        instance-zone: dev
        acl-token: 27f5ed83-3a3e-fd2e-baa0-d075ad5508fb
        health-check-interval: 15s
        tags: env=DEV, profile=dev
      config:
        enabled: true
        prefix: bootconfig
        defaultContext: app
        format: FILES
        acl-token: 27f5ed83-3a3e-fd2e-baa0-d075ad5508fb